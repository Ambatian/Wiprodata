import React from "react";
import { Container, Typography } from "@mui/material";

const EmployeesPage = () => {
  return (
    <Container sx={{ mt: 10 }}>
      <Typography variant="h5">👥 Employees Page</Typography>
    </Container>
  );
};

export default EmployeesPage;
