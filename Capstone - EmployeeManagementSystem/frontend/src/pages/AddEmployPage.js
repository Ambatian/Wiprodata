import React from "react";
import { Container, Typography } from "@mui/material";

const AddEmployeePage = () => {
  return (
    <Container sx={{ mt: 10 }}>
      <Typography variant="h5">➕ Add Employee Page</Typography>
    </Container>
  );
};

export default AddEmployeePage;
