import React from "react";
import { Container, Typography } from "@mui/material";

const PendingLeavesPage = () => {
  return (
    <Container sx={{ mt: 10 }}>
      <Typography variant="h5">📄 Pending Leaves Page</Typography>
    </Container>
  );
};

export default PendingLeavesPage;
